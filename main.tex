% --------------------------
% Document class
% --------------------------
\documentclass[
A4paper,            1 paper size A4
oneside,            % oneside or twoside printing
openright,          % doublepage cleaning ends up right side
10pt,               % font size
headings=normal,    % size of headings
titlepage=on        % own page for each title page
]{article}
% **************************************************
% **************************************************
\newcommand{\University}{\protect{Universidad Polit\'ecnica de Madrid}}
\newcommand{\UNIVERSITY}{\protect{UNIVERSIDAD POLIT\'ECNICA DE MADRID}}
\newcommand{\thesisTitle}{\protect {Entrega final Sistemas no Lineales}}
\newcommand{\thesisDate}{\today}
\newcommand{\UPMCentre}{\protect {Escuela Técnica Superior de Ingenieros Industriales}}

\newcommand\blankpage{%
    \null
    \thispagestyle{empty}%
    \addtocounter{page}{-1}%
    \newpage}
\renewcommand{\familydefault}{\sfdefault}
% **************************************************
% Settings
% **************************************************
% This line pulls in all your packages and biblatex settings

\input{settings} 
\usepackage[numbers,sort&compress]{natbib}
\bibliographystyle{IEEEtran}

\usepackage{subcaption}
\usepackage{graphicx} % Seguramente ya lo tienes
\usepackage{float}    % Para usar la opción [H]

\usepackage[table]{xcolor}
\usepackage{colortbl}
\definecolor{gris}{RGB}{242,242,242}

% TikZ packages for diagrams
\usepackage{tikz}
\usetikzlibrary{calc,arrows.meta,decorations.pathmorphing,patterns,angles,quotes}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% **************************************************
% Begin document
% **************************************************

\begin{document}

% --------------------------
% Cover
% --------------------------

\pagestyle{empty}                % no header nor footer
\input{cover}  
\newpage
\selectlanguage{english}
\setcounter{tocdepth}{3}        % define depth of toc
\tableofcontents                % display table of contents
\clearpage

% --------------------------
% Main matter
% --------------------------
\pagenumbering{arabic}
\setcounter{page}{1}
\pagestyle{fancy}

\section{Introducción}
El objetivo del proyecto de la asignatura de sistemas no lineales  es automatizar el juego de la recreativa Monza, un sistema dinámico altamente no lineal.Se considero para el presente trabajo poder trabajar con las plataformas de Python y Matlab. El objetivo del juego consiste en controlar la trayectoria de una moneda que se introduce en el carril superior, guiándola a través de los distintos niveles hasta alcanzar el carril inferior y depositarla en una apertura de salida específica. Debido a su topología, cada carril está delimitado por dos extremos críticos: uno de ellos permite la transición de la moneda hacia el nivel inmediatamente inferior (manteniendo la continuidad del sistema), mientras que el extremo opuesto provoca la expulsión de la moneda hacia el exterior, lo que constituye la condición de fallo del juego.

% PLACEHOLDER: Imagen de la máquina real o esquema general
\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/Monza1.png}
    \caption{Estructura y Niveles del Juego del Monza.}
    \label{fig:niveles}
\end{figure}



% PLACEHOLDER: Imagen de la máquina real o esquema general
\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/simulation.png}
    \caption{Niveles de dificultad de la recreativa Monza y disposición de los rieles.}
    \label{fig:niveles}
\end{figure}

\section{Modelado Matemático del Sistema}
Para simular el comportamiento de la moneda, se han derivado las ecuaciones físicas que rigen su movimiento en las distintas fases del juego.

\subsection{Geometría del Tablero}
Los rieles se modelan como parábolas desfasadas verticalmente. La ecuación general en el sistema de referencia del tablero $(x_1, y_1)$ es:
\begin{equation}
    y_1 = -0.54 \cdot x_1^2 + C
\end{equation}
Donde $C$ varía según el nivel del piso. El ángulo de inclinación local $\phi(x)$ se determina mediante la derivada de la curva. La pendiente local se calcula numéricamente mediante:
\begin{equation}
    \text{slope}(x) = \frac{\nabla y(x)}{\nabla x(x)}
\end{equation}
donde $\nabla$ representa el operador gradiente discreto. El ángulo de inclinación local se obtiene como:
\begin{equation}
    \alpha(x) = \arctan(\text{slope}(x))
\end{equation}



\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        scale=5,
        >=Stealth, % Better looking arrowheads
        every node/.style={font=\small} % Consistent font size
    ]
        % --- 1. Definitions ---
        \def\xval{0.3} % The x-coordinate of the point
        \def\yval{-0.54*\xval*\xval + 0.2} % The function f(x)
        \def\slope{-1.08*\xval} % The derivative f'(x)
        
        % --- 2. Axes ---
        \draw[->] (-0.6,0) -- (0.65,0) node[right] {$x_l$};
        \draw[->] (0,-0.1) -- (0,0.5) node[left] {$y_l$};

        % --- 3. Parabola Plot ---
        \draw[thick, blue, domain=-0.55:0.55, samples=200] 
            plot (\x, {-0.54*\x*\x + 0.2});

        % --- 4. The Point (Moneda) ---
        \coordinate (P) at (\xval, \yval);
        \fill[red] (P) circle (1pt);
        % Use a pin label to avoid overlap with the curve/tangent
        \node[pin={100:Moneda}, inner sep=1pt] at (P) {};

        % --- 5. Tangent Line ---
        % Calculate points for the tangent segment using the slope
        % Tangent vector is (1, slope). We go backwards and forwards from P.
        \draw[green!60!black, thick] 
            ($(P) - 0.25*(1, \slope)$) -- ($(P) + 0.25*(1, \slope)$)
            node[right, text=green!40!black] {Tangente};

        % --- 6. Angle Alpha ---
        % Define a horizontal point and a point on the tangent for the angle
        \coordinate (H) at ($(P) + (0.3, 0)$); % Horizontal right
        \coordinate (T) at ($(P) + (0.3, 0.3*\slope)$); % Point on tangent
        
        % Draw the horizontal helper line (dashed for clarity)
        \draw[orange, dashed] (P) -- (H);
        
        % Draw the angle arc
        \pic[
            draw=orange, 
            thick,
            "$\alpha$", 
            angle radius=0.6cm, 
            angle eccentricity=1.2
        ] {angle = T--P--H};

        % --- 7. Labels & Dimensions ---
        
        % Slope equation (Moved to top right to avoid axis overlap)
        \node[align=left, fill=white, inner sep=2pt, text=black!80] at (0.35, 0.4) {
            $\text{slope} = \frac{dy}{dx} = -1.08x$
        };
        
        % C parameter (Vertical offset) - Drawn as a dimension line
        \draw[dashed, gray] (0, 0.2) -- (-0.1, 0.2); % Projection to left
        \draw[|<->|, gray] (-0.08, 0) -- (-0.08, 0.2) node[midway, left] {$C$};

    \end{tikzpicture}
    \caption{Parámetros geométricos de las parábolas que conforman los rieles.}
    \label{fig:geometria}
\end{figure}

\subsection{Dinámica del Movimiento en el Carril}
El movimiento de la moneda considera fuerzas de gravedad, fricción viscosa con el aire, rozamiento con el carril y contacto con la pared lateral.

Se distinguen dos modos de movimiento:
\begin{itemize}
    \item \textbf{Deslizamiento puro:} Cuando la fricción es insuficiente para generar rodadura.
    \item \textbf{Rodadura sin deslizamiento:} Condición regida por el coeficiente de fricción estática $\mu_s$.
\end{itemize}

Las ecuaciones dinámicas en el sistema no inercial incluyen la fuerza centrífuga y de Coriolis debido a la rotación $\theta$ del tablero.

\subsubsection{Transformaciones de Coordenadas}
Para transformar entre el sistema de coordenadas local del tablero $(x_l, y_l)$ y el sistema global $(x_g, y_g)$, se utilizan las siguientes rotaciones:

\begin{equation}
    \begin{bmatrix} x_g \\ y_g \end{bmatrix} = \begin{bmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{bmatrix} \begin{bmatrix} x_l \\ y_l \end{bmatrix}
\end{equation}

La transformación inversa (global a local) viene dada por:
\begin{equation}
    \begin{bmatrix} x_l \\ y_l \end{bmatrix} = \begin{bmatrix} \cos\theta & \sin\theta \\ -\sin\theta & \cos\theta \end{bmatrix} \begin{bmatrix} x_g \\ y_g \end{bmatrix}
\end{equation}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[scale=2.5, >=Stealth]
        % Global coordinate system
        \draw[->, thick, black] (0,0) -- (1.5,0) node[right] {$x_g$};
        \draw[->, thick, black] (0,0) -- (0,1.5) node[above] {$y_g$};
        \node[below left] at (0,0) {O};
        
        % Local coordinate system (rotated by theta)
        \def\theta{30}
        \draw[->, thick, blue] (0,0) -- ({1.3*cos(\theta)}, {1.3*sin(\theta)}) 
            node[above right] {$x_l$};
        \draw[->, thick, blue] (0,0) -- ({-1.3*sin(\theta)}, {1.3*cos(\theta)}) 
            node[above left] {$y_l$};
        
        % Draw angle arc
        \draw[->, gray] (0.4,0) arc (0:\theta:0.4);
        \node[gray] at ({0.5*cos(\theta/2)}, {0.5*sin(\theta/2)}) {$\theta$};
        
        % Example point in local coordinates
        \coordinate (P) at ({0.8*cos(\theta) - 0.3*sin(\theta)}, {0.8*sin(\theta) + 0.3*cos(\theta)});
        \fill[red] (P) circle (1.5pt) node[above right] {P};
        
        % Projections to local axes
        \draw[dashed, blue!50] (P) -- ({0.8*cos(\theta)}, {0.8*sin(\theta)});
        \draw[dashed, blue!50] (P) -- ({-0.3*sin(\theta)}, {0.3*cos(\theta)});
        
        % Projections to global axes
        \draw[dashed, black!50] (P) -- ({0.8*cos(\theta) - 0.3*sin(\theta)}, 0);
        \draw[dashed, black!50] (P) -- (0, {0.8*sin(\theta) + 0.3*cos(\theta)});
        
        % Labels
        \node[blue] at ({0.4*cos(\theta)}, {0.4*sin(\theta)}) [below] {$x_l$};
        \node[blue] at ({-0.15*sin(\theta)}, {0.15*cos(\theta)}) [left] {$y_l$};
    \end{tikzpicture}
    \caption{Transformación de coordenadas entre el sistema local (rotado) y el sistema global.}
    \label{fig:coordenadas}
\end{figure}

\subsubsection{Ecuaciones de Movimiento en el Carril}
Durante el movimiento de rodadura, la aceleración tangencial local se calcula como:
\begin{equation}
    a_l = -g \sin(\alpha + \theta) - \mu_v v_l
\end{equation}
donde $g$ es la aceleración gravitatoria, $\alpha$ es el ángulo de inclinación local del riel, $\theta$ es el ángulo de rotación del tablero, $\mu_v$ es el coeficiente de fricción viscosa y $v_l$ es la velocidad local tangencial.

La integración temporal se realiza mediante el método de Euler:
\begin{align}
    v_l(t+\Delta t) &= v_l(t) + a_l(t) \Delta t \\
    x_l(t+\Delta t) &= x_l(t) + v_l(t) \Delta t \\
    y_l(t+\Delta t) &= f(x_l(t+\Delta t))
\end{align}
donde $f(x)$ es la función que describe la geometría del riel (interpolación cúbica de los puntos discretos).



\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        scale=7,
        >=Stealth, % Improved arrowheads
        every node/.style={font=\small}
    ]
        % --- 1. Parameters ---
        \def\a{-0.54} % Curvature
        \def\c{0.15}  % Vertical offset
        \def\xpos{0.25} % Position of the ball (moved slightly for better visibility)
        
        % Calculate y position and slope angle explicitly
        \pgfmathsetmacro{\ypos}{\a*\xpos*\xpos + \c}
        \pgfmathsetmacro{\slope}{2*\a*\xpos}
        \pgfmathsetmacro{\angleval}{atan(\slope)} % The angle of the tangent

        % --- 2. Coordinate System Axes (Background) ---
        \draw[->, gray!50, dashed] (-0.5, 0) -- (0.5, 0) node[right, black] {$x_l$};
        \draw[->, gray!50, dashed] (0, -0.05) -- (0, 0.4) node[above, black] {$y_l$};

        % --- 3. The Track ---
        \draw[thick, blue, domain=-0.45:0.45, samples=100] 
            plot (\x, {\a*\x*\x + \c});

        % --- 4. The Ball ---
        \coordinate (P) at (\xpos, \ypos);
        % Draw ball with slight transparency to see origin if needed, or solid
        \fill[red!20] (P) circle (1pt); 
        \draw[red!80] (P) circle (1pt);

        % --- 5. Helper Lines (Tangent & Normal Axis) ---
        % These help show why the vectors point where they do
        \begin{scope}[shift={(P)}, rotate=\angleval]
            \draw[gray, dotted] (-0.4, 0) -- (0.4, 0); % Tangent line
            \draw[gray, dotted] (0, -0.1) -- (0, 0.3); % Normal line
        \end{scope}

        % --- 6. Forces ---
        
        % Weight (mg) - Always straight down
        \draw[->, thick, red!80!black] (P) -- ($(P) + (0, -0.25)$) 
            node[right] {$mg$};

        % Normal Force (N) - Perpendicular to slope (90 degrees from tangent)
        \draw[->, thick, green!50!black] (P) -- ($(P) + ({0.25*cos(\angleval+90)}, {0.25*sin(\angleval+90)})$) 
            node[above right] {$\mathbf{N}$};

        % Friction (Ff) - Opposite to velocity (assuming moving right, friction points left-up)
        % Note: If x > 0 and moving away, slope is negative. 
        % Friction acts along the tangent line.
        \draw[->, thick, orange!90!black] (P) -- ($(P) + ({-0.25*cos(\angleval)}, {-0.25*sin(\angleval)})$) 
            node[midway, above, sloped, text=orange!90!black] {$F_f = \mu_v v$};

        % --- 7. Angles ---
        % Define horizontal and tangent points for angle drawing
        \coordinate (H) at ($(P) + (0.3, 0)$); % Horizontal right
        \coordinate (T) at ($(P) + ({0.3*cos(\angleval)}, {0.3*sin(\angleval)})$); % Along tangent
        
        \draw[dashed, gray] (P) -- (H); % Horizontal ref
        
        % Draw angle arc
        \pic[
            draw=black!70, 
            <->, 
            angle radius=1.5cm, 
            "$\alpha + \theta$", 
            angle eccentricity=1.4
        ] {angle = T--P--H};

    \end{tikzpicture}
    \caption{Diagrama de cuerpo libre de la moneda sobre el riel parabólico.}
    \label{fig:fuerzas}
\end{figure}

\subsection{Modelado de Caída y Rebote}
Cuando la moneda abandona un riel, entra en una fase de caída libre hasta impactar con el siguiente nivel. El impacto se modela como un choque inelástico caracterizado por un coeficiente de restitución $k$, perdiendo energía en cada rebote.

\subsubsection{Transición de Rodadura a Caída Libre}
Al abandonar un riel, la velocidad global se calcula combinando la velocidad local transformada y la velocidad tangencial debida a la rotación del tablero:
\begin{align}
    v_{x,\text{loc}} &= v_l \\
    v_{y,\text{loc}} &= \text{slope} \cdot v_l \\
    \begin{bmatrix} v_{x,\text{rel}} \\ v_{y,\text{rel}} \end{bmatrix} &= \begin{bmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{bmatrix} \begin{bmatrix} v_{x,\text{loc}} \\ v_{y,\text{loc}} \end{bmatrix} \\
    v_{x,\text{tan}} &= -\dot{\theta} \cdot y_g \\
    v_{y,\text{tan}} &= \dot{\theta} \cdot x_g
\end{align}
La velocidad global inicial de caída es:
\begin{equation}
    \begin{bmatrix} v_{x,g} \\ v_{y,g} \end{bmatrix} = \begin{bmatrix} v_{x,\text{rel}} \\ v_{y,\text{rel}} \end{bmatrix} + \begin{bmatrix} v_{x,\text{tan}} \\ v_{y,\text{tan}} \end{bmatrix}
\end{equation}

\subsubsection{Dinámica de Caída Libre}
Durante la caída, el movimiento se rige por:
\begin{align}
    v_{y,g}(t+\Delta t) &= v_{y,g}(t) - g \Delta t \\
    x_g(t+\Delta t) &= x_g(t) + v_{x,g}(t) \Delta t \\
    y_g(t+\Delta t) &= y_g(t) + v_{y,g}(t) \Delta t
\end{align}

\subsubsection{Resolución de Colisiones}
Al detectar una colisión con un riel, se calcula el vector normal a la superficie:
\begin{align}
    \phi &= \arctan(\text{slope}) + \theta \\
    \mathbf{n} &= \begin{bmatrix} -\sin\phi \\ \cos\phi \end{bmatrix}
\end{align}

La componente normal de la velocidad es:
\begin{equation}
    v_n = \mathbf{v}_g \cdot \mathbf{n} = v_{x,g} n_x + v_{y,g} n_y
\end{equation}

El impulso de rebote se calcula mediante:
\begin{equation}
    J = -(1 + k) v_n
\end{equation}
donde $k$ es el coeficiente de restitución. La velocidad después del impacto es:
\begin{equation}
    \begin{bmatrix} v_{x,g}' \\ v_{y,g}' \end{bmatrix} = \begin{bmatrix} v_{x,g} \\ v_{y,g} \end{bmatrix} + J \mathbf{n}
\end{equation}

Si la velocidad normal de impacto es suficientemente pequeña ($|v_n| < 0.5$ m/s), la moneda se adhiere al riel y retorna al estado de rodadura, proyectando la velocidad global sobre la tangente local:
\begin{equation}
    v_l = \mathbf{v}_g' \cdot \mathbf{t} = v_{x,g}' \cos\phi + v_{y,g}' \sin\phi
\end{equation}
donde $\mathbf{t} = [\cos\phi, \sin\phi]^T$ es el vector tangente unitario.

\section{Identificación de Parámetros}
Los parámetros físicos del modelo han sido ajustados experimentalmente para coincidir con el comportamiento de la caja negra de Simulink. La Tabla~\ref{tab:parametros} muestra los valores utilizados en la implementación del código Python.

\begin{table}[H]
    \centering
    \caption{Parámetros del modelo físico utilizados en la simulación.}
    \label{tab:parametros}
    \begin{tabular}{lcc}
        \toprule
        \textbf{Parámetro} & \textbf{Símbolo} & \textbf{Valor} \\
        \midrule
        Gravedad & $g$ & 9.81 m/s$^2$ \\
        Coef. fricción viscosa & $\mu_v$ & 0.01 \\
        Coef. restitución choque & $k$ & 0.1 \\
        Paso temporal (física) & $\Delta t$ & 0.033 s \\
        Sub-pasos físicos & $n_{\text{sub}}$ & 10 \\
        Paso temporal interno & $\delta t = \Delta t/n_{\text{sub}}$ & 0.0033 s \\
        Ángulo máximo de inclinación & $\theta_{\max}$ & 45$^\circ$ (0.785 rad) \\
        Velocidad angular máxima & $\omega_{\max}$ & 8.0 rad/s \\
        \bottomrule
    \end{tabular}
\end{table}

Los parámetros físicos fundamentales son la gravedad $g = 9.81$ m/s$^2$, el coeficiente de fricción viscosa $\mu_v = 0.01$ que modela la resistencia al movimiento, y el coeficiente de restitución $k = 0.1$ que determina la pérdida de energía en los choques. El paso temporal de simulación $\Delta t = 0.033$ s corresponde a una frecuencia de muestreo de aproximadamente 30 Hz, mientras que internamente se utilizan $n_{\text{sub}} = 10$ sub-pasos para mayor precisión en la integración numérica, resultando en un paso temporal interno de $\delta t = 0.0033$ s.

Para el control del sistema, se establece un límite máximo de inclinación del tablero de $\theta_{\max} = 45^\circ$ y una velocidad angular máxima de $\omega_{\max} = 8.0$ rad/s para evitar movimientos bruscos. El setpoint de posición se fija en $x_{\text{sp}} = 0.0$ m, correspondiente al centro del tablero.

Para evaluar el comportamiento de nuestro modelo, se hizo un registro de la trayectoria que traza bajo el estado inicial a un ángulo de 0.12 rad, figura \ref{fig:trayectoriatest}. Se comparó ambos datos y muestran un comportamiento similar tanto en el estado de deslizamiento del carril y el estado de caída, al igual que en la recepción. 
\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/comparacion.png}
    
    \caption{Comparación de trayectorias entre modelo de simulink y modelo propio desde el punto inicial con un ángulo de -0.12 rad.}
    \label{fig:trayectoriatest}
\end{figure}


\section{Estrategias de Control Implementadas}
Se han diseñado e implementado controladores para gobernar el ángulo $\theta$ del tablero y asegurar que la moneda complete el recorrido.

\subsection{Control por Modos Deslizantes (SMC)}

El Control por Modos Deslizantes (SMC,Sliding Mode Control) es una técnica de control robusto no lineal  bajo el paradigma de los Sistemas de Estructura Variable (VSS). A diferencia de las leyes de control continuo convencionales, el SMC emplea una ley de control discontinua de alta frecuencia que altera la estructura dinámica del sistema en función del estado actual del mismo . El controlador SMC busca llevar el estado del sistema a una superficie de deslizamiento $s(t) = 0$. Se definen las variables de error $e$ (posición respecto al objetivo) y $\dot{e}$.
\cite{shtessel2014sliding},\cite{Adaptive_slidingmode}.
\begin{equation}
    s(t) = e(t) + \lambda \cdot \dot{e}(t)
\end{equation}
La ley de control aplicada es del tipo conmutada:
\begin{equation}
    u = -k \cdot \text{sign}(s(t))
\end{equation}
El algoritmo sigue una lógica discreta para calcular la acción de control $u$ en cada paso de tiempo $t$. Las ecuaciones que rigen el sistema son:

\textbf{Cálculo del Error ($e$):} Diferencia entre la posición objetivo y la actual.
\begin{equation}
    e(t) = x_{\text{target}} - x_{\text{coin}}
\end{equation}

\textbf{Derivada del Error ($\dot{e}$):} Aproximación discreta del cambio del error.
\begin{equation}
    \dot{e}(t) \approx e(t) - e(t-1)
\end{equation}

\textbf{Superficie de Deslizamiento ($s$):} Combinación lineal del error y su derivada.
\begin{equation}
    s(t) = e(t) + \lambda \cdot \dot{e}(t)
\end{equation}


\begin{table}[h]
    \centering
    \caption{Definición de Perfiles de Control para el Controlador SMC}
    \label{tab:perfiles_control_smc}
    \renewcommand{\arraystretch}{1.4} % Ajusta el espaciado vertical
    \begin{tabular}{@{}l c c p{6.5cm}@{}}
    \toprule
    \textbf{Perfil de Control} & \textbf{$\lambda$} & \textbf{$k$} & \textbf{Caracterización Dinámica} \\ \midrule
    
    \textbf{Conservador} & 100 & 0.0020 & 
    \textit{Baja Ganancia.} Prioriza la estabilidad y la suavidad del actuador. El tiempo de establecimiento es alto, pero elimina el riesgo de oscilaciones. \\ 
    
    \textbf{Nominal} & 100 & 0.0040 & 
    \textit{Balanceado.} Punto de operación estándar. Ofrece un compromiso óptimo entre velocidad de respuesta y consumo de control. \\ 
    
    \textbf{Respuesta Rápida} & 110 & 0.0095 & 
    \textit{Alta Ganancia.} Maximiza la velocidad de convergencia ($\dot{e}$). Adecuado para perturbaciones rápidas, aunque susceptible al \textit{chattering}. \\ 
    
    \textbf{Alta Precisión} & 140 & 0.0095 & 
    \textit{Control Robusto.} Combina alta ganancia con un fuerte término de amortiguamiento ($\lambda$). Minimiza el error de estado estacionario en trayectorias complejas. \\ \bottomrule
    \end{tabular}
\end{table}



\begin{figure}[H]
    \centering
    \begin{tikzpicture}[scale=1, >=Stealth]
        % Draw axes
        \draw[->] (-2.5,0) -- (2.5,0) node[right] {$e$};
        \draw[->] (0,-2.5) -- (0,2.5) node[above] {$\dot{e}$};
        
        % Draw sliding surface s = e + lambda*e_dot = 0
        \def\lambda{0.5}
        \draw[thick, blue] (-2.5, {2.5/\lambda}) -- (2.5, {-2.5/\lambda}) 
            node[above right] {$s = e + \lambda\dot{e} = 0$};
        
        % Draw region labels
        \node[blue!50] at (-1.5, 1.5) {$s > 0$};
        \node[blue!50] at (1.5, -1.5) {$s < 0$};
        
        % Draw trajectory approaching sliding surface
        \draw[thick, red, ->] (-2, 2) .. controls (-1, 1.5) and (-0.5, 0.8) .. 
            (-0.3, 0.3) .. controls (-0.1, 0.1) .. (0, 0);
        \draw[thick, red, ->] (2, -2) .. controls (1, -1.5) and (0.5, -0.8) .. 
            (0.3, -0.3) .. controls (0.1, -0.1) .. (0, 0);
        
        % Mark origin
        \fill[black] (0,0) circle (1.5pt) node[below left] {Origen};
        
        % Draw reaching phase arrows
        \draw[->, green!60!black, thick] (-1.8, 1.8) -- (-1.2, 1.2) 
            node[midway, above left] {Fase de alcance};
        \draw[->, green!60!black, thick] (1.8, -1.8) -- (1.2, -1.2) 
            node[midway, below right] {Fase de alcance};
        
        % Draw sliding phase
        \draw[->, orange, thick] (-0.5, 0.25) -- (0, 0) 
            node[midway, above] {Fase de deslizamiento};
        
        % Add grid
        \draw[gray!30, very thin] (-2.5,-2.5) grid (2.5,2.5);
    \end{tikzpicture}
    \caption{Trayectoria en el espacio de estados hacia la superficie de deslizamiento.}
    \label{fig:smc}
\end{figure}

\subsection{Control de Lógica Difusa (Fuzzy Logic)}
La Lógica Difusa (Fuzzy Logic), propuesta por Lotfi Zadeh en 1965, es una extensión de la lógica booleana clásica que permite modelar la imprecisión y la incertidumbre inherentes al razonamiento humano y a los sistemas físicos complejos. A diferencia de la lógica clásica, donde un elemento pertenece o no a un conjunto (valores discretos $\{0, 1\}$), la lógica difusa contiene d el concepto de grado de pertenencia, permitiendo que un elemento pertenezca parcialmente a un conjunto con un valor en el intervalo continuo $[0, 1]$.


Se implementó un controlador tipo Fuzzy basado en reglas heurísticas.

\begin{itemize}
    \item \textbf{Entrada 1 - Error:} Error de posición $e = x_{\text{setpoint}} - x_g$ con 9 funciones de pertenencia triangulares.
    \item \textbf{Entrada 2 - Velocidad:} Velocidad local tangencial $v_l$ de la moneda con 5 funciones de pertenencia (triangulares y trapezoidales infinitas en los bordes).
    \item \textbf{Salida - Inclinación:} Ángulo objetivo $\theta_{\text{target}}$ del tablero con 9 funciones de pertenencia triangulares.
    \item \textbf{Total de Reglas:} 20 reglas difusas que relacionan las entradas con la salida.
\end{itemize}

\subsubsection{Funciones de Pertenencia}

\paragraph{Función Triangular (trimf)}
Para la mayoría de las funciones de pertenencia se utiliza la función triangular, definida como:
\begin{equation}
    \mu(x; a, b, c) = \begin{cases}
        0 & \text{si } x \leq a \text{ o } x \geq c \\
        \frac{x-a}{b-a} & \text{si } a < x < b \\
        \frac{c-x}{c-b} & \text{si } b \leq x < c
    \end{cases}
\end{equation}
donde $a$, $b$ y $c$ son los vértices izquierdo, central (pico) y derecho del triángulo, respectivamente.

\paragraph{Función Trapezoidal Infinita (trapinf)}
Para las funciones de pertenencia en los bordes del dominio de velocidad (rapidaNeg y rapidaPos), se utiliza una función trapezoidal que se extiende hasta infinito:
\begin{equation}
    \mu_{\text{trapinf}}(x; a, b, c, \text{izquierda}) = \begin{cases}
        1 & \text{si } x \leq b \text{ (borde izquierdo)} \\
        \frac{c-x}{c-b} & \text{si } b < x < c \\
        0 & \text{si } x \geq c
    \end{cases}
\end{equation}
\begin{equation}
    \mu_{\text{trapinf}}(x; a, b, c, \text{derecha}) = \begin{cases}
        0 & \text{si } x \leq a \\
        \frac{x-a}{b-a} & \text{si } a < x < b \\
        1 & \text{si } x \geq b \text{ (borde derecho)}
    \end{cases}
\end{equation}

\subsubsection{Especificación de las Funciones de Pertenencia}

\paragraph{Entrada 1: Error de Posición}
El error de posición tiene 9 funciones de pertenencia triangulares con los siguientes parámetros:
\begin{align}
    \text{LargeNeg:} &\quad [-0.35, -0.29, -0.23] \\
    \text{MediumNeg:} &\quad [-0.28, -0.22, -0.16] \\
    \text{SmallNeg:} &\quad [-0.21, -0.15, -0.08] \\
    \text{XSmallPequeñoNeg:} &\quad [-0.10, -0.05, -0.01] \\
    \text{cero:} &\quad [-0.01, 0, 0.01] \\
    \text{XSmallPos:} &\quad [0.01, 0.05, 0.10] \\
    \text{SmallPos:} &\quad [0.08, 0.15, 0.21] \\
    \text{MediumPos:} &\quad [0.16, 0.22, 0.28] \\
    \text{LargePos:} &\quad [0.23, 0.29, 0.35]
\end{align}
\paragraph{Entrada 2: Velocidad Local}
La velocidad local tangencial tiene 5 funciones de pertenencia. Las funciones de los bordes utilizan trapinf, mientras que las centrales son triangulares.
\begin{align}
    \text{FastNeg:} &\quad [-8.5, -0.2125, -0.17] \quad \text{(trapinf izquierdo)} \\
    \text{SlowNeg:} &\quad [-0.17, -0.085, 0] \\
    \text{cero:} &\quad [-0.255, 0, 0.255] \\
    \text{SlowPos:} &\quad [0, 0.085, 0.17] \\
    \text{FastPos:} &\quad [0.17, 0.2125, 8.5] \quad \text{(trapinf derecho)}
\end{align}

\paragraph{Salida: Inclinación}
La inclinación del tablero tiene 9 funciones de pertenencia triangulares:
\begin{align} \text{LargeTurnNeg:} &\quad [-0.43497, -0.36, -0.28503] \\ \text{MediumTurnNeg:} &\quad [-0.34497, -0.27, -0.19503] \\ \text{SmallTurnNeg:} &\quad [-0.25497, -0.18, -0.10503] \\ \text{XSmallTurnNeg:} &\quad [-0.16497, -0.09, -0.01503] \\ 
\text{cero:} &\quad [-0.07497, 0, 0.07497] \\ 
\text{XSmallTurnPos:} &\quad [0.01503, 0.09, 0.16497] \\ 
\text{SmallTurnPos:} &\quad [0.10503, 0.18, 0.25497] \\ 
\text{MediumTurnPos:} &\quad [0.19503, 0.27, 0.34497] \\ \text{LargeTurnPos:} &\quad [0.28503, 0.36, 0.43497] \end{align}

\subsubsection{Evaluación de Reglas y Inferencia}
El sistema implementa 20 reglas difusas de la forma:
\begin{equation}
    R_i: \text{IF } e \text{ is } E_i \text{ AND } v_l \text{ is } V_i \text{ THEN } u \text{ is } U_i
\end{equation}

Para cada regla, la fuerza de activación se calcula mediante el operador mínimo (AND):
\begin{equation}
    w_i = \min(\mu_{E_i}(e), \mu_{V_i}(v_l))
\end{equation}

El método de implicación utilizado es el mínimo (Mamdani), donde la salida de cada regla se recorta por su fuerza de activación:
\begin{equation}
    \mu_{U_i}^{\text{recortada}}(u) = \min(w_i, \mu_{U_i}(u))
\end{equation}

La agregación de todas las reglas activas se realiza mediante el operador máximo:
\begin{equation}
    \mu_{\text{agregada}}(u) = \max_{i=1}^{20} \mu_{U_i}^{\text{recortada}}(u)
\end{equation}

\subsubsection{Defuzzificación}
La salida defuzzificada se obtiene mediante el método del centroide con una resolución de 1000 puntos:
\begin{equation}
    u = \frac{\int_{u_{\min}}^{u_{\max}} u \cdot \mu_{\text{agregada}}(u) \, du}{\int_{u_{\min}}^{u_{\max}} \mu_{\text{agregada}}(u) \, du}
\end{equation}

En la implementación discreta, esto se calcula como:
\begin{equation}
    u = \frac{\sum_{j=1}^{N} u_j \cdot \mu_{\text{agregada}}(u_j)}{\sum_{j=1}^{N} \mu_{\text{agregada}}(u_j)}
\end{equation}
donde $N = 1000$ es la resolución de defuzzificación y $u_j$ son puntos equiespaciados en el rango de salida. Si el denominador es cero, la salida es $u = 0$.

\subsubsection{Conversión a Ángulo Objetivo y Cinemática del Actuador}
El ángulo objetivo del tablero se calcula limitando la salida defuzzificada:
\begin{equation}
    \theta_{\text{target}} = \text{clip}(u, -\theta_{\max}, \theta_{\max})
\end{equation}
donde $\theta_{\max} = 45°$ ($0.785$ rad) es el límite máximo de inclinación.

El controlador limita la velocidad angular del actuador mediante:
\begin{equation}
    \omega = \text{clip}\left(\frac{\theta_{\text{target}} - \theta}{\Delta t}, -\omega_{\max}, \omega_{\max}\right)
\end{equation}
donde $\omega_{\max} = 3$ rad/s es la velocidad angular máxima permitida. La actualización del ángulo se realiza mediante integración de Euler:
\begin{equation}
    \theta(t+\Delta t) = \theta(t) + \omega \Delta t
\end{equation}



\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/fuzzysets.png}

    \caption{Representación esquemática de las funciones de pertenencia del controlador difuso.}
    \label{fig:fuzzy}
\end{figure}


\section{Resultados de Simulación}
Se realizaron pruebas comparativas de los controladores en los cuatro niveles de dificultad del juego.

\subsection{Comparativa de Trayectorias}
A continuación, se muestra la trayectoria resultante de la moneda bajo la acción del control en el nivel de dificultad máxima.


\subsubsection{Sliding mode}

\begin{figure}[H]
    \centering
    % --- IMAGEN IZQUIERDA ---
    % Usamos 0.45\textwidth (45% del ancho) para que quepan las dos.
    \includegraphics[width=0.45\textwidth]{figures/Test_1_smc.png}
    %
    \hfill % <--- ESTE COMANDO ES CLAVE: Rellena el espacio entre ellas
    %
    % --- IMAGEN DERECHA ---
    % Asegúrate de cambiar el nombre por el de tu segunda imagen
    \includegraphics[width=0.45\textwidth]{figures/Test_2_smc.png}
    
    \caption{Comparación de la trayectoria de Dificultad 1 (izquierda) y Dificultad 2 (derecha).}
    \label{fig:trayectorias_lado_a_lado}
\end{figure}

\begin{figure}[H]
    \centering
    % --- IMAGEN IZQUIERDA ---
    % Usamos 0.45\textwidth (45% del ancho) para que quepan las dos.
    \includegraphics[width=0.45\textwidth]{figures/Test_3_smc.png}
    %
    \hfill % <--- ESTE COMANDO ES CLAVE: Rellena el espacio entre ellas
    %
    % --- IMAGEN DERECHA ---
    % Asegúrate de cambiar el nombre por el de tu segunda imagen
    \includegraphics[width=0.45\textwidth]{figures/Test_4_smc.png}
    
    \caption{Comparación de la trayectoria de Dificultad 3 (izquierda) y Dificultad 4 (derecha).}
    \label{fig:trayectorias_lado_a_lado}
\end{figure}


\subsubsection{Fuzzy logic}
Con el controlador Fuzzy se logró completar los 4 niveles de dificultad tanto en el modelo de mpython y el modelo de maltab, acá se dejan los resultados.
\begin{figure}[H]
    \centering
    % Row 1
    \includegraphics[width=0.4\textwidth]{figures/dificultad1-comp.png}
    \hfill
    \includegraphics[width=0.4\textwidth]{figures/dificultad2-comp.png}
    
    \vspace{0.5cm} % Space between rows
    
    % Row 2
    \includegraphics[width=0.4\textwidth]{figures/dificultad3-comp.png}
    \hfill
    \includegraphics[width=0.4\textwidth]{figures/dificultad4-comp.png}
    
    \caption{Comparación de las trayectorias de nuestro modelo y en simulink (Dificultad 1-4).}
    \label{fig:grid_simple}
\end{figure}


\subsection{Comportamiento}
De los experimentos anteriores se obtienen las siguientes gráficas que nos permiten conocer mejor el comportamiento del sistema.


\subsubsection{Sliding mode}

% PLACEHOLDER: Gráficas de Theta y Theta_dot
\begin{figure}[H]
    \centering
    % \includegraphics[width=0.8\textwidth]{ruta/a/control_signals.png}
    \fbox{\begin{minipage}{0.8\textwidth}
        \centering
        \vspace{1cm}
        \textbf{[INSERTAR IMAGEN: Gráficas de ángulo y velocidad angular]}
        \vspace{1cm}
    \end{minipage}}
    \caption{Evolución del ángulo de inclinación y la velocidad angular durante la partida.}
    \label{fig:actuador}
\end{figure}

\subsubsection{Fuzzy}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{results/platform_angle.png}
    \caption{Evolución del ángulo de inclinación de la plataforma durante la partida.}
    \label{fig:fuzzy_platform_angle}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{results/control_output.png}
    \caption{Salida del controlador difuso (inclinación objetivo) durante la simulación.}
    \label{fig:fuzzy_control_output}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{results/position_tracking.png}
    \caption{Seguimiento de posición: setpoint, posición actual y error del sistema.}
    \label{fig:fuzzy_position_tracking}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{results/velocity.png}
    \caption{Evolución de la velocidad global y local de la moneda durante la simulación.}
    \label{fig:fuzzy_velocity}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{results/phase_plot.png}
    \caption{Diagrama de fase mostrando la trayectoria del sistema en el espacio de estados (error vs. velocidad).}
    \label{fig:fuzzy_phase_plot}
\end{figure}

\subsection{Tiempos de Ejecución}
La siguiente tabla resume el rendimiento de los controladores medido en tiempo necesario para completar el juego.

% PLACEHOLDER: Tabla de Tiempos
\begin{table}[H]
    \centering
    \caption{Tiempo (en segundos) para completar la partida según dificultad y controlador.}
    \label{tab:tiempos}
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Controlador} & \textbf{Dif. 1} & \textbf{Dif. 2} & \textbf{Dif. 3} & \textbf{Dif. 4} \\
        \midrule
        Sliding Mode (SMC) & 6.59 & 8.79 & 13.32 & 15.91 \\
        Fuzzy Logic & 4.36 & 7.65 & 10.22 & 13.13 \\
        \bottomrule
    \end{tabular}
    \vspace{0.2cm}
    \\ \footnotesize{(Datos extraídos de la Tabla 5.1 del PDF 1)}
\end{table}

\section{Conclusiones}
Se ha logrado desarrollar un simulador fiel en Python que reproduce la dinámica no lineal de la recreativa Monza.
\begin{itemize}
    \item El modelo físico incluye fricción compleja y rebotes inelásticos, permitiendo una simulación realista.
    \item El controlador \textbf{Fuzzy} demostró ser el más rápido y robusto en términos generales debido a su naturaleza intuitiva y agresiva.
    \item El controlador \textbf{SMC} garantiza estabilidad teórica pero resultó ser más conservador y lento.
\end{itemize}

Como limitación principal, el modelo actual permite aceleraciones angulares infinitas en el actuador. Una línea futura de trabajo sería incluir la dinámica del motor (par) para restringir la aceleración $\alpha$, acercando aún más la simulación a la realidad física.
\bibliography{sample}
\end{document}